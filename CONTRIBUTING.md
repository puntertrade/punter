# Contributing

Thanks for looking. Punter is early — issues and PRs welcome.

1. Fork, branch off `main`.
2. `npm install && npm run build && npm test`.
3. Keep the core deterministic — feed/oracle adapters are injected, never hard-wired.
4. Conventional commits (`feat:`, `fix:`, `chore:` ...).
